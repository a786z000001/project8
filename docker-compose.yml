version: "3.9"

services:
  mongo:
    image: mongo
    ports:
      - "27017:27017"

  backend:
    build: ./backend
    ports:
      - "5000:5000"
    depends_on:
      - mongo
    environment:
      - MONGO_URI=mongodb://mongo:27017/taskdb
      - PORT=5000
      - JWT_SECRET=supersecret

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend